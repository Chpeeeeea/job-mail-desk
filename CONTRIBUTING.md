# Contributing

1. Use anonymized fixtures only.
2. Preserve read-only IMAP behavior.
3. Add regression tests for parser or linking changes.
4. Keep public research data separate from private mail data.
5. Run `python -m unittest discover -s tests -v` before submitting changes.
